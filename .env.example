# Email Validator Environment Variables
# Copy this file to .env.local and update the values as needed

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Use * to allow all origins (not recommended for production)
# Example: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# =============================================================================
# Rate Limiting
# =============================================================================
# Note: These are currently defined in src/lib/constants.ts
# Future: Move to environment variables for production flexibility
# RATE_LIMIT_SINGLE_MAX=100
# RATE_LIMIT_SINGLE_WINDOW_MS=60000
# RATE_LIMIT_BULK_MAX=10
# RATE_LIMIT_BULK_WINDOW_MS=60000
# MAX_BULK_SIZE=1000

# Trusted IPs that bypass rate limiting (comma-separated)
# Useful for internal services, monitoring systems, or load balancers
# Supports individual IPs and /24 CIDR ranges
# Examples:
#   TRUSTED_IPS=10.0.0.1,192.168.1.100
#   TRUSTED_IPS=10.0.0.0/24,192.168.1.0/24
TRUSTED_IPS=

# =============================================================================
# API Authentication
# =============================================================================
# Enable/disable API key requirement for external requests
# When true, external API requests require a valid API key
# Same-origin requests (from the frontend) are always allowed
API_KEY_REQUIRED=false

# API Keys Configuration
# Format: key:name:permissions:tier:expiresAt (separated by semicolons)
#
# - key: The API key string (use a secure random string)
# - name: A friendly name for the key
# - permissions: Comma-separated list of: validate, bulk, admin
# - tier: Rate limit tier - free (100/min), pro (1000/min), enterprise (10000/min)
# - expiresAt: Optional expiration date (ISO 8601 format)
#
# Examples:
# Single key with validate permission:
#   API_KEYS=abc123:MyApp:validate:free
#
# Multiple keys with different permissions:
#   API_KEYS=key1:ProdApp:validate,bulk:pro;key2:TestApp:validate:free
#
# Key with expiration:
#   API_KEYS=key1:TempKey:validate:free:2025-12-31
#
API_KEYS=

# =============================================================================
# Security Configuration
# =============================================================================
# CSP violation report endpoint (defaults to /api/csp-report)
# In production, you might want to use an external service like Sentry
# CSP_REPORT_URI=/api/csp-report

# =============================================================================
# External Services (Optional)
# =============================================================================
# Custom DNS server for MX lookups (defaults to Google DNS)
# DNS_SERVER=https://dns.google/resolve

# =============================================================================
# Logging and Debugging
# =============================================================================
# Set to 'true' to enable verbose logging
# DEBUG=false
